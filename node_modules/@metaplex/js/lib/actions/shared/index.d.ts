import { Connection, Keypair, PublicKey } from '@solana/web3.js';
import { Transaction } from '../../programs';
interface MintTxs {
    mint: Keypair;
    recipient: PublicKey;
    createMintTx: Transaction;
    createAssociatedTokenAccountTx: Transaction;
    mintToTx: Transaction;
}
export declare function prepareTokenAccountAndMintTx(connection: Connection, owner: PublicKey): Promise<MintTxs>;
export {};
